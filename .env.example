# Infra-only env vars. Domain bot settings (token, timezone, …) live in the DB.
# SQLite only today, for example:
# DATABASE_URL=sqlite:///adhoc_history.sqlite3
# SQLITE_PATH=adhoc_history.sqlite3
# postgres/postgresql DATABASE_URL values are not supported yet.
